# 徐汇区物业投诉治理数字化分析项目 - 架构说明

> 本文档供 AI 智能体快速理解项目结构、数据口径和报告体系。

---

## 1. 项目概述

徐汇区 12345 热线物业投诉工单的数字化分析项目，覆盖 2024-2026 年（截至 2026 年 8 月）共 **66,812 条**工单数据，产出 11 份 HTML 分析报告 + 数据底座 + 脚本工具链。

### 核心数据
- 数据时间：2024年1月 - 2026年8月
- 工单总量：66,812 条（2024: 29,468 / 2025: 23,618 / 2026年1-8月: 13,726）
- 覆盖街道：13个（漕河泾、长桥、徐家汇、田林、枫林路、斜土路、龙华、康健新村、天平路、湖南路、虹梅路、凌云路、华泾镇）
- 问题分类：14类（停车管理、房屋维修、邻里纠纷、房屋违规使用、业主大会/业委会、消防管理、群租管理、物业安保、旧住房改造、清洁卫生、物业收费、物业服务态度、房屋交易纠纷、其他）

---

## 2. 数据口径（重要）

| 口径 | 说明 | 适用报告 |
|------|------|---------|
| **热线全量口径** | 按热线工单中 `community_name` 字段直接统计，不与纳统小区匹配。覆盖 1,374 个小区 | 除根因报告外的所有报告 |
| **纳统小区口径** | 以 `纳统小区.xls`（994个小区）为基准，将热线数据匹配后分析。覆盖 968 个小区 | 仅 `物业投诉根因分析报告` |

> **规则：** 只有物业投诉根因分析报告使用纳统小区口径，其他所有报告均按热线全量口径。两套口径不互通，不要混用。

### 2.1 投诉密度口径（统一为「件/千户」）

> **投诉密度 = 投诉量 ÷ 户数 × 1000（件/千户）**

全站所有分析报告的密度指标一律按此规则计算。历史版本曾用过「件/百户」「件/小区」，
现均已换算为件/千户；「件/小区」仅在街镇报告密度表与根因报告街道分布图中作为**对照列**保留，
不再称作密度。

| 项目 | 说明 |
|------|------|
| 户数据源 | `data/community_linked_data.pkl` 的 `total_households` 字段（994 个纳统小区档案） |
| 注意 | **工单主数据 `merged_cleaned.pkl` 和 SQLite 库 `work_orders` 都没有户数字段**，算密度必须关联 `community_linked_data.pkl` |
| 覆盖率 | 994 个小区中 991 个有户数；与热线小区匹配率 95.3%，工单覆盖 97.2% |
| 街镇报告分母 | 该街镇「2026年1-8月有投诉小区」的户数合计（与有投诉小区数口径一致） |
| 全区基准 | 27.7 件/千户（13,719 件 ÷ 494,966 户） |

换算口径时的注意：**比例类表述不受影响**（如「是…的 2.4 倍」「低 14.7%」「同比 -23.2%」），
只有绝对数值需要换算（件/百户 → 件/千户 为 ×10）。

### 2.2 2026 年统计期口径（统一为「1-8月」）

> **所有报告中 2026 年的统计一律为 2026年1-8月，禁止再出现「2026年上半年 / 2026H1」口径。**

| 规则 | 说明 |
|------|------|
| 总数与同比 | 2026年1-8月 vs 2025年1-8月（同期口径），全区 13,726 vs 15,798 = **-13.1%** |
| 全年同比 | 2025全年 vs 2024全年（-19.9%）保持不变，与 1-8月同比并存，注意区分标签 |
| 历史遗留 | 《数据质量优化专项报告》中「2025-2026H1」是**原始数据批次名**（非统计期），保留不改 |
| 别名映射 | 底部抬升名单匹配时：钦州路111弄小区→钦州路111弄、漓江花园→漓江花园（领馆壹号院），78/84 匹配 |

底部抬升报告重算脚本：`scripts/recalc_bottom_lift_8m.py`（产出 `bottom_lift_2026_8m.json`，
内含对旧上半年口径 4,510/4,263/2,057/1,662 的回归校验）+ `scripts/update_bottom_lift_report.py`（回填 HTML，幂等）。

### 2.3 词云规范（边界统一为 16:9 长方形）

> **所有「诉求主题词」词云一律使用 16:9 长方形边界，不再使用行政区域形状遮罩。**

| 项 | 规范 |
|------|------|
| 边界 | 16:9 长方形。实现：**不传 `maskImage`**（插件缺省时布局区域即满矩形）+ `keepAspect: false` |
| 字号 | 随容器尺寸缩放：报告内嵌容器 980×551 用 `sizeRange [39,147]`；导出画布 1600×900 用 `[64,240]` |
| 内容 | 每街镇 Top24 词；`rotationRange [0,0]`（中文不旋转）；配色 `WC_PALETTE` 8 色按权重循环 |
| 报告内嵌 | `gen_street_report.py :: chart_wordcloud()`，容器 `.chart-box.wordcloud` |
| 独立出图 | `scripts/export_wordclouds.py` → `词云图/<街镇>.png`（1600×900）+ `词云图/词云总览.html` |
| 本地依赖 | `assets/vendor/`（echarts 5.4.3 + echarts-wordcloud 2.1.0），离线可渲染 |

**易踩的坑**：`.chart-box { height:360px }` 会**覆盖** `aspect-ratio`（有显式高度时 aspect-ratio 不生效），
所以词云容器必须显式写 `height:auto`，否则容器变成 980×360（2.72:1）而非 16:9。

历史遗留：`scripts/gen_xuhui_mask.py`、`assets/xuhui_mask.*`、`data/geo/xuhui_district_310104.json`
为早期「徐汇区行政边界形状词云」资产，现已不在报告中引用（保留备查）。

---

## 3. 目录结构

```
8-24 徐汇课题一期/
├── PROJECT_GUIDE.md              ← 本文件（项目架构说明）
├── 课题成果总览.html              ← 首页/入口页（含导航到所有子报告）
│
├── ── 核心成果报告（5份）──
├── 12345投诉量下降数据报告.html    ← #1 下降趋势、月度同比、街道排名
├── 街镇专项分析报告.html            ← #1·附 13个街镇单独分析（趋势/归因/热点/重复/风险，切换栏吸顶）
├── 投诉去重识别方法论说明.html      ← #2 去重方法论
├── 2026年1-8月投诉去重建议清单.html ← #2·附 去重清单（月度合集，含8个月份Tab）
├── 数据质量优化专项报告.html        ← #3 数据清洗与质量
├── 高风险小区预警清单.html          ← #6 风险评分Top30
├── 高风险小区预警清单.xlsx
│
├── ── 深度分析报告（5份）──
├── 物业投诉根因分析报告.html        ← #4 六维根因（纳统口径）
├── 重点问题专题分析报告（5份）.html  ← #5 停车/维修/邻里/违规/业委会
├── 治理效果追踪分析报告.html        ← #7 街道+14类改善率追踪
├── 底部抬升小区热线数据分析报告.html  ← #7·附 底部抬升84小区分析
│
├── ── 月度/综合 ──
├── 数据分析月报合集.html            ← 2024.01-2026.08 月度卡片合集
├── 多源数据清洗与关联分析报告.html    ← 数据清洗与关联过程
├── 徐汇AI智能体.html                ← AI问答界面（H5）
├── 版本更新记录.html
├── 验收演练报告.html
├── 微信真机测试指南.html
│
├── ── Word文档 ──
├── 徐汇区物业投诉治理数字化分析项目内容介绍.docx
│
├── data/                          ← 数据底座（核心）
│   ├── merged_cleaned.pkl         ← ★ 主数据集（66,812条，热线全量）
│   ├── xuhui_complaints.db        ← SQLite原始数据库
│   ├── community_linked_data.pkl  ← 纳统小区关联数据（根因报告专用）
│   ├── governance_tracking_hotline.pkl ← 治理追踪数据
│   ├── data_dictionary.json       ← 字段字典
│   ├── data_link_quality.json     ← 数据关联质量报告
│   ├── top30_aug.pkl              ← 1-8月Top30小区
│   ├── worsened_aug.pkl           ← 1-8月恶化Top10
│   ├── cat_aug_analysis.pkl       ← 1-8月分类分析
│   ├── street_aug_analysis.pkl    ← 1-8月街道分析
│   └── merged_cleaned_sample.csv  ← 样本CSV（前1000行预览）
│
├── 热线数据/                       ← 原始Excel数据源
│   ├── 2024年市局14类.xlsx
│   ├── 2025 年全年、2026年 1-6 月市局14类.xlsx
│   ├── 2026 年7月市局14类.xlsx
│   └── 2026 年8月市局14类.xlsx
│
├── 徐汇各科室业务数据/              ← 多源业务数据
│   ├── 纳统小区.xls               ← 994个纳统小区基准表
│   ├── 电梯基本信息.xlsx
│   ├── 修缮项目.xlsx
│   ├── 底部抬升84个小区0403.xlsx
│   ├── 门牌幢.xlsx
│   ├── 2026年1-6月五查.xlsx
│   ├── 征收.xlsx
│   ├── 拆房信息.xlsx
│   └── 测绘基本信息.xlsx
│
├── scripts/                       ← Python脚本 + JSON缓存
│   ├── build_database.py          ← 构建SQLite数据库
│   ├── clean_data.py             ← 数据清洗（产出merged_cleaned.pkl）
│   ├── governance_tracking.py    ← 治理效果追踪数据生成
│   ├── risk_scoring.py           ← 高风险小区评分
│   ├── topic_analysis.py         ← 5个专题分析
│   ├── data_quality_audit.py     ← 数据质量审计
│   ├── analyze_complaint_trends.py ← 投诉趋势分析
│   ├── precompute.py             ← 预计算缓存
│   ├── gen_demo_cache.py         ← 演示缓存生成
│   ├── h1_dedup_analysis.py      ← 上半年去重分析
│   ├── june_dedup_analysis.py    ← 6月去重分析
│   ├── dedup_monthly.py          ← 月度去重分析（四重策略，可逐月扩展，2026-08起使用）
│   ├── gen_dedup_list_html.py    ← 去重建议清单页生成器（数据→HTML，可复现）
│   ├── build_street_dataset.py   ← 街镇维度数据集（13镇×7组维度 → street_analysis.json）
│   ├── gen_street_report.py      ← 街镇专项分析页生成器（数据→HTML，可复现）
│   ├── update_report_street_section.py ← 下降报告街道章节数据修正（可重复执行）
│   ├── dedup_list_style.html     ← 清单页样式片段（生成器引用）
│   ├── dedup_list_script.html    ← 清单页交互脚本片段（生成器引用）
│   ├── run_demo_rehearsal.py     ← 演示排练
│   └── *.json                    ← 各脚本输出的JSON缓存
│
├── src/                           ← AI智能体后端代码
│   ├── config.py
│   ├── main.py
│   ├── llm_client.py
│   ├── intent_classifier.py
│   ├── query_functions.py
│   ├── cache_manager.py
│   └── session_manager.py
│
├── frontend/                      ← AI智能体前端
│   ├── index.html
│   ├── app.js
│   └── style.css
│
├── 月度分析报告/                   ← 历史月度Word报告（19份）
├── 年度分析报告/                   ← 年度报告（街道/集团维度）
│   ├── 街道/
│   └── 集团/
│
├── demo_cache/                    ← 演示缓存
├── logs/                          ← 日志
└── tests/                         ← 测试
```

---

## 4. 核心数据结构

### 4.1 主数据集：`data/merged_cleaned.pkl`

DataFrame，66,812 行 × 21 列：

| 列名 | 类型 | 说明 |
|------|------|------|
| `order_id` | str | 工单编号 |
| `accept_time` | str | 受理时间 |
| `source` | str | 来源 |
| `address` | str | 投诉地址（原始） |
| `order_type` | str | 工单类型（投诉举报类/求助类） |
| `content` | str | 诉求内容 |
| `handler` | str | 处理人 |
| `reply` | str | 回复内容 |
| `year` | int | 年份（2024/2025/2026） |
| `month` | int | 月份（1-12） |
| `community_id` | str | 小区ID（纳统匹配） |
| `community_name` | str | 小区名称（清洗后） |
| `community_addr` | str | 小区地址 |
| `property_company` | str | 物业公司 |
| `street` | str | 街道（13个） |
| `category_14` | str | 14类分类 |
| `level1`-`level5` | str | 多级分类标签 |

### 4.2 纳统小区关联数据：`data/community_linked_data.pkl`

994 个纳统小区 × 45 个字段（含电梯、修缮、失信、底部抬升等业务数据关联），仅根因分析报告使用。

### 4.3 数据加载示例

```python
import pandas as pd

# 主数据集（热线全量口径）
df = pd.read_pickle('data/merged_cleaned.pkl')

# 纳统小区关联数据（纳统口径，根因报告专用）
linked = pd.read_pickle('data/community_linked_data.pkl')

# 按年份/月份筛选
df_2026_18 = df[(df['year']==2026) & (df['month']<=8)]  # 2026年1-8月

# 按街道统计
street_count = df_2026_18.groupby('street').size().sort_values(ascending=False)

# 按类别统计
cat_count = df_2026_18.groupby('category_14').size().sort_values(ascending=False)
```

---

## 5. 报告体系与导航

### 5.1 入口页

**课题成果总览.html** — 首页，含核心数字卡片 + 报告导航卡片，点击进入各子报告。

### 5.2 导航栏

所有子报告顶部均有统一的下拉导航栏：
- **核心成果 ▾** — 悬停展开 #1~#6 报告链接
- **深度分析 ▾** — 悬停展开 #4~#7 报告链接
- **← 返回总览** — 返回首页

### 5.3 报告清单

| 编号 | 文件名 | 内容 | 数据口径 |
|------|--------|------|---------|
| #1 | 12345投诉量下降数据报告.html | 总量趋势、月度同比(4色)、街道排名(含单月)、分类排名 | 全量 |
| #1·附 | 街镇专项分析报告.html | 13个街镇单独画像（街镇切换分页置顶 + 全区总表/对比/贡献度/热力） | 全量 |
| #2 | 投诉去重识别方法论说明.html | 去重算法、相似度评分模型 | 全量 |
| #2·附 | 2026年1-8月投诉去重建议清单.html | 按月更新的去重建议清单（1-8月Tab合集） | 全量 |
| #3 | 数据质量优化专项报告.html | 数据清洗规则、字段标准化、关联质量 | 全量 |
| #4 | 物业投诉根因分析报告.html | 六维根因（硬件/管理/业态/费用/规模/历史） | **纳统** |
| #5 | 重点问题专题分析报告（5份）.html | 停车/维修/邻里/违规/业委会 5个专题 | 全量 |
| #6 | 高风险小区预警清单.html | 三年累计评分 + 1-8月近期预警Top30 | 全量 |
| #7 | 治理效果追踪分析报告.html | 街道+14类改善率追踪(含2026同期) | 全量 |
| #7·附 | 底部抬升小区热线数据分析报告.html | 84个底部抬升小区专项 | 全量 |
| - | 数据分析月报合集.html | 2024.01-2026.08 月度卡片（可点击切换） | 全量 |

---

## 6. 数据更新流程

当有新的月份数据时，按以下步骤更新：

### 步骤1：添加原始数据
将新的 Excel 文件放入 `热线数据/` 目录（命名格式：`2026年X月市局14类.xlsx`）。

### 步骤2：运行数据清洗
```bash
cd "8-24 徐汇课题一期"
python3 scripts/clean_data.py
```
产出更新后的 `data/merged_cleaned.pkl`。

### 步骤3：更新去重建议清单（月度合集）
```bash
python3 scripts/dedup_monthly.py --month 9        # 新月份去重分析，自动并入 dedup_monthly_all.json
python3 scripts/gen_dedup_list_html.py            # 重新生成 2026年1-N月投诉去重建议清单.html
```
> 依赖：jieba（分词）。生成器会覆盖同名合集页；1-7月为历史发布口径，8月起为脚本可复现口径。

### 步骤4：更新报告数据
各报告的月度数据直接内嵌在 HTML 中（静态数字），需要手动或脚本更新。关键更新点：
- 月度趋势表格：新增行
- 街道排名表格：更新1-N月累计数 + 新增单月列
- 分类排名表格：更新1-N月累计数
- 核心数字卡片：更新总量
- ECharts 图表：在 `data` 数组中填入新月份的值
- **街道章节可用脚本自动修正**（含核心卡片、副标题、街道对比图、6.2街道表）：
```bash
python3 scripts/update_report_street_section.py --dry-run   # 先看将要改什么
python3 scripts/update_report_street_section.py             # 实际写入
```
- **全区口径提醒**：核心卡片用全区全量（含街镇归属为空），街道图表/表用13街镇明细，两者不可混用
- 街道章节的数字若手工改，务必用「同期口径」（1-N月 vs 1-N月），历史上曾出现把 1-7月 数值标成 1-8月 的问题

### 步骤4b：更新街镇专项分析报告（#1·附）
```bash
python3 scripts/build_street_dataset.py    # 用最新主数据集重算13街镇×7组维度
python3 scripts/gen_street_report.py       # 重新生成 街镇专项分析报告.html
```
> 依赖：jieba（分词，可选，缺失时自动回退）。去重率按「逐月独立去重后汇总」口径计算，
> 与《去重方法论》《去重建议清单》可比；不可把多月合并成池再跑去重（会让重复率虚高到 55%+）。

### 步骤5：验证数据一致性
```python
# 验证全区总量
df = pd.read_pickle('data/merged_cleaned.pkl')
total = len(df[(df['year']==2026) & (df['month']<=8)])  # 应与报告中数字一致
```

---

## 7. 关键数字速查（截至2026年8月）

| 指标 | 数值 |
|------|------|
| 工单总量（三年） | 66,812 件 |
| 2024年全年 | 29,468 件 |
| 2025年全年 | 23,618 件（同比 -19.9%） |
| 2026年1-8月 | 13,726 件（同比 -13.1%） |
| 2026年8月单月 | 2,242 件（同比 +2.8%，今年首次转正） |
| 纳统小区数 | 994 个 |
| 热线全量小区数 | 1,374 个 |
| 物业企业数 | 200+ 家 |
| 街道数 | 13 个 |
| 问题分类数 | 14 类 |

---

## 8. 技术栈

- **数据分析**：Python 3 + pandas + numpy
- **可视化**：ECharts 5.4.3（CDN引入，内嵌HTML）
- **报告格式**：纯 HTML（自包含，无外部依赖，可直接浏览器打开）
- **数据库**：SQLite（`data/xuhui_complaints.db`）
- **AI后端**：Python（`src/`目录，含LLM客户端、意图分类、会话管理）
- **AI前端**：HTML + JS（`frontend/`目录）
- **版本控制**：Git（GitHub: sanjune/xuhui_research2026）

---

## 9. 注意事项

1. **口径不要混**：根因报告用纳统口径，其他报告用全量口径。两套口径数字不一致是正常的。
2. **月份完整性**：2026年只有1-8月数据，9-12月没有。图表中9-12月位置应为 `null`，不是 0。
3. **数字格式**：报告中千分位用逗号（如 13,726），百分比保留1位小数（如 -13.1%）。
4. **导航栏**：所有子报告共用一套下拉导航CSS和HTML，修改时需同步更新所有文件。
5. **报告自包含**：每个HTML文件都是独立的，内嵌CSS和JS，不依赖外部文件（除ECharts CDN）。
